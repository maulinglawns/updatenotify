This is a shell script that notifies the user on
updates (via a cron job, more on that later).

I wrote this because the "official" notifier does 
not work when running Xfce on top of Debian Wheezy.

More later...
